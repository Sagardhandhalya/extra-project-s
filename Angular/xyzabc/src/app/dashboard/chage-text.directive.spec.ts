import { ChageTextDirective } from './chage-text.directive';

describe('ChageTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChageTextDirective();
    expect(directive).toBeTruthy();
  });
});
